name: heart
description: 'Extension methods for Strings and Iterables, inspired by Haskell'
version: 0.6.0
repository: 'https://github.com/MadSimple/heart/'

environment:
  sdk: '>=2.19.0 <4.0.0'

dependencies:
  collection: ^1.19.1

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.31.1

